import 'package:flutter/material.dart';

class SearchDiscoverPage extends StatelessWidget {
  const SearchDiscoverPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Search & Discover'));
  }
}
